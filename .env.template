# Gemini API Key (Required for extraction)
# Get one from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Firecrawl API Key (Required for --agent mode, recommended for best crawling)
# Get one from: https://firecrawl.dev/
# Agent mode uses Spark models: 'mini' (default, cost-efficient) or 'pro' (complex tasks)
FIRECRAWL_API_KEY=your_firecrawl_api_key_here
